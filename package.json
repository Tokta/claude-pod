{
  "name": "claude-pod",
  "version": "2.0.0",
  "description": "Run Claude Code in a Docker sandbox scoped to one project folder.",
  "private": true,
  "type": "module",
  "bin": {
    "claude-pod": "bin/claude-pod.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "license": "MIT"
}
